import { TextContentDirective } from './text-content.directive';

describe('SimplyTextDirective', () => {
	it('should create an instance', () => {
		const directive = new TextContentDirective();
		expect(directive).toBeTruthy();
	});
});
