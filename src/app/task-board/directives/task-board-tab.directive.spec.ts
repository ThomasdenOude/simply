import { TaskBoardTabDirective } from './task-board-tab.directive';

describe('TaskBoardTabDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskBoardTabDirective();
    expect(directive).toBeTruthy();
  });
});
