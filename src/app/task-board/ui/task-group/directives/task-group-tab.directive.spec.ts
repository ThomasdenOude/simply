import { TaskGroupTabDirective } from './task-group-tab.directive';

describe('TaskBoardTabDirective', () => {
	it('should create an instance', () => {
		const directive = new TaskGroupTabDirective();
		expect(directive).toBeTruthy();
	});
});
