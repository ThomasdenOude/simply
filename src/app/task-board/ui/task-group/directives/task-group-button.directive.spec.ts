import { TaskGroupAddButtonDirective } from './task-group-add-button.directive';

describe('TaskGroupButtonDirective', () => {
	it('should create an instance', () => {
		const directive = new TaskGroupAddButtonDirective();
		expect(directive).toBeTruthy();
	});
});
